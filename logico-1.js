// and(lógica e) = &&, todas as condições precisam ser verdadeiro
// or (lógica ou) = ||, somente quando todas as condições forem negativas
// exclusive xor (lógica ou exclusivo) = (!=), somente quando as condições forem oposta

const a = 3;
const b = 7;

console.log(a > 0 != b > 0);
